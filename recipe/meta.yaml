{% set name = "empy" %}
{% set version = "4.2.1" %}
{% set sha256 = "48f7cff0aedfd275c6702d11959fda023384b048497dbe517dec13ea1ae3af12" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: http://www.alcyone.com/software/empy/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv
  noarch: python

requirements:
  host:
    - python
    - pip
  run:
    - python

test:
  imports:
    - em

about:
  home: http://www.alcyone.com/software/empy/
  license: LGPL-2.1
  license_family: GPL
  license_file: LICENSE
  summary: 'A powerful and robust templating system for Python.'
  dev_url: http://www.alcyone.com/software/empy/

extra:
  recipe-maintainers:
    - wolfv
